<%= form_for @member, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Member</legend>

    <div class="control-group">
      <%= f.label :family, :class => 'control-label' %>
      <div class="controls">
        <%= f.collection_select :family_id, Family.all, :id, :name %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :bills_paid, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :bills_paid, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :balance, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :balance, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', members_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
